<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <%= link_to "勤怠システム", current_user.nil? ? root_path : @current_user, id: "logo" %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "トップへ", current_user.nil? ? root_path : @current_user %></li>
        <% if current_user.nil? %>
          <li><%= link_to "ログイン", login_path %></li>
        <% else %>
          <li><%= link_to "ユーザー一覧", users_path if @current_user.admin? %></li>
          <li><%= link_to "基本情報の修正", edit_basic_info_user_path(@current_user) if @current_user.admin? %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              アカウント <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "個人情報編集", edit_user_path(@current_user) %></li>
              <li class="divider"></li>
              <li><%= link_to "ログアウト", logout_path, method: :delete %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>